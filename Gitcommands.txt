git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/aznchat100/dd.git
git push -u origin master


Origins(Links file to github)
git remote add origin https://github.com/username/myproject.git


git status
git add Readme.txt (To make Git notice that the file is there)
git commit -m “Add Readme.txt”


git pull = git fetch + git merge

Branches
git checkout -b <name_of_branch> (create new branch and checks into it)=
git branch <name_of_branch> + git checkout <name_of_branch>


git push origin <name_of_branch> (Push the branch on github)


# View just remote-tracking branches
git branch --remotes
git branch -r

# View both strictly local as well as remote-tracking branches
git branch --all
git branch -a

Deleting a remote branch:

git push origin --delete <branch>  # Git version 1.7.0 or newer


Deleting a local branch:

git branch --delete <branch>
git branch -d <branch> # Shorter version
git branch -D <branch> # Force delete un-merged branches